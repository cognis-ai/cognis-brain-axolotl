# Cognis Brain — Axolotl brand assets

CLI-only. Brand surfaces in this fork: `Dockerfile.cognis` env defaults, `src/axolotl/cognis/branding/banner.py` ASCII banner, `COGNIS-README.md`. No upstream Python edited.

Reserved for future visual assets if a v1.5 web UI lands.
